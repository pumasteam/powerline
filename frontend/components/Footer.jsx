const Footer = () => {
  return (
    <footer className="flex items-center justify-center">
      <p className="text-center p-8 font-bold">{`©️ ${new Date().getFullYear()} PowerLine - Made with ❤️ by Pumas Team`}</p>
    </footer>
  );
};

export default Footer;
